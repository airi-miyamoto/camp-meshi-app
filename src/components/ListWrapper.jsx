import { GiCampfire } from "react-icons/gi";
import campMeshiLogo from '../assets/camp-meshi-logo.svg';

function ListWrapper({ children }) {
    return (
        <section className="max-w-[970px] mx-auto">
            {children}
        </section>
    )
}

export default ListWrapper